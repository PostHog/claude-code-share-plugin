{
  "slashCommandRun": [
    {
      "name": "share-handler",
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/scripts/share_session.py",
        "${COMMAND_ARGS}"
      ],
      "filter": {
        "slashCommands": ["share"]
      }
    }
  ]
}
